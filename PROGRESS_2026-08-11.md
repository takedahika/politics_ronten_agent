# 論点の現在地 — 今日の進捗と積み残し
> 2026-08-11（作業メモ / Gitにプッシュしない）

---

## ✅ 今日やったこと

### 初期トピックとワークフローの整理
- [x] Discord通知の安定化（`bash`の文字列結合エラーを解消し、`jq`による安全なJSON生成方式へ移行）
- [x] `propose_topics.py` にGeminiを活用して5つのMarkdown初期コンテンツを生成するロジックを追加
- [x] トピック更新時（`analyze.py`）、最新のニュースや出来事がヘッダー直下（一番上）に挿入される降順ロジックを実装
- [x] 古いダミー情報を含むトピック群（diet-seat-reduction等）と処理済みログ（.processed_essays.json）を完全に削除・リセット

### 問題の特定
- Substackの `article_url` を用いたスクレイピングがボット対策（JavaScript要求や404エラー）により失敗することが判明。
- 対策として、**GAS経由で受信メール本文を直接渡す方式**、またはActions画面での `article_content` への直接コピペが必須であることを確認。
- Geminiが初期ページを生成する際、Web検索機能がないため**URL（一次資料リンク）を捏造してしまう**ハルシネーション問題を確認。

---

## 🔲 積み残し（次回やること）

### 1. 新規トピック初期生成時の「本物のURL」取得実装（Geminiネイティブ検索）
**背景**: 
現在、新しい論点を立ち上げる際、Geminiが背景や事実関係を生成しているが、Web検索機能がないためURLがすべてダミー（適当な文字列）になっている。また、初期調査においては国会議事録よりも、論文（ac.jp）や公的機関（go.jp）の資料を参照する方が適している。

**作業内容**:
- 外部の検索APIやスクレイピングライブラリは使用せず、Gemini APIがネイティブに提供している **Google Search Grounding（Google検索グラウンディング）** 機能を有効化する（`tools="google_search"`）。
- プロンプトにて、「学術論文（ac.jp）や公的機関（go.jp）の情報を優先して検索し、その内容を読み込んだ上で、実在する正確なURLを出典として必ず記載すること」を指示する。
- これにより、Geminiが自ら検索と熟読を行い、ダミーではない本物のURL付きのMarkdownコンテンツを自動生成できるようになる。

### 2. UIおよび設定ファイルの改修（ニュースレター連携）
**背景**: 
トピック生成時に、トリガーとなったSubstackのURLが `topic.yaml` に引き継がれておらず、UI上のリンクがダミーになっている。またUIの文言も改善が必要。

**作業内容**:
- `scripts/propose_topics.py` を修正し、GAS等から受け取った実在の `essay_url` と `essay_title` を `topic.yaml` の `newsletter_articles` に正しく保存するようにする。
- `site/src/app/topics/[slug]/page.tsx` を修正：
  - 一番上のラベル「✍️ ニュースレター連動エッセイ」を「✍️ ニュースレター」に変更。
  - 固定の「エッセイ『1年に3日だけ政治を考える』で取り上げられました。」というテキストを、「このトピックは以下のエッセイを起点に調査が開始されました。」等の汎用的なテキストに変更。

### 3. Substack自動検知（GAS実装）の継続
**背景**:
Substack記事のURLスクレイピングがブロックされることが確定したため、Gmailから直接本文を引っ張ってくるGASの重要性がより高まった。

**作業内容**:
- `02_Homemade_Apps/34_substack_gmail_trigger/` にGASプロジェクトを作成し、GmailのSubstackメールを受信したら本文を抽出。
- GitHub API（workflow_dispatch）を叩き、`article_content` と `article_original_url` を直接渡す仕組みを完成させる。

### 4. 【Post-MVP】ユーザー参加型（UGC）ファクト追加機能の構築
**背景**:
現在のMVP構成を超えた将来的な機能として、サイトを訪れた一般ユーザーが特定の「論点」に対し、AIが拾い切れていない新しい事実やソースを投稿（提案）できる仕組みを作りたい。

**作業内容（実装案）**:
- 各論点のページ内に、「この論点に関する新しい事実を提案する」といったボタン（Googleフォームへのリンク）を設置する。
- その際、URLパラメータ等を使って「どの論点（slug）への提案か」を自動的にフォームに紐付ける。
- ユーザーがフォームから情報（事実、ソースURLなど）を送信すると、GASを経由してGitHub Actions（`repository_dispatch` 等）をトリガーする。
- 起動したAIエージェントが送信内容を検証・成形し、該当トピックのMarkdown（`facts.md`等）に追記する形のPull Request（PR）を自動作成する。これを管理者がレビュー・マージすることでサイトに反映される。

**懸念点・考慮すべき事項**:
- **スパム・荒らし対策**: 誰でも送信できる公開フォームの場合、スパムが大量に送られるリスクがある。AIのプロンプトによるフィルタリングと、人間（管理者）によるPRの厳格なレビューが必須。
- **ファクトチェックの精度**: ユーザーが持ち込んだソースURLをAIが正確に解読し、真偽を判定（Grounding）できるかどうかのチューニングが重要になる。
- **インフラ・セキュリティ**: 外部（Googleフォーム）からGitHub APIを叩いてPRを作る経路となるため、悪意のあるリクエストでActionが過剰に起動しないような設計が必要。

---

## 📝 システム全体の現状整理（次回方針）

```
GAS（メール受信） ※次回実装・必須
  ↓ (article_content を直接渡す)
propose_topics.yml (GitHub Actions)
  ↓
propose_topics.py
  ├─ GeminiネイティブのGoogle検索グラウンディング機能で本物の学術・公的ソースを取得 ※次回実装
  └─ Geminiが検索結果を用いて、本物のURL付きの初期Markdown 5ファイルを生成
  ↓
PR作成 → 人間が承認

-----------------------
毎日 6:00 UTC (daily.yml)
  ↓
collect.py → ニュースRSS ＋ 国会議事録APIで最新動向を収集
  ↓
Gemini がタイムラインの一番上（降順）に追記するPRを作成
```
