# Progress Report: 政治論点エージェント プロジェクト

**最終更新日**: 2026-08-14

---

## ✅ 2026-08-12 の成果

**1. グローバルルールの策定と適用（トーン＆マナーの徹底）**
- [x] 「AIが〜します」というメタ言及の禁止、高校3年生でも理解できる平易な言葉遣い（「乏しい」「属し」などの官公庁用語の禁止）といったルールを `GEMINI.md` に明文化。
- [x] 既存の論点記事（国旗損壊罪など）に対して、この平易化ルールを適用しリライトを実施。

**2. GitHub PR 自動修正エージェント（AXIS 3）の実装**
- [x] PRのコメント欄で `@ronten-ai` と指示を出すことで、Geminiが自動修正・コミットを行う `pr_comment_agent.py` と Workflow を作成。
- [x] Gemini SDK の新旧混在エラーを解消し、`gemini-3.5-flash` に統一。

**3. プルリクエスト（PR）画面での人間向けコンテキストの拡充**
- [x] 新規論点提案PRの本文に、AIの抽出概要・元エッセイURLを追加。
- [x] 読者情報提供PRの本文に、ニュース要約とAIの修正内容を追加。

---

## ✅ 2026-08-13 の成果

**4. 論点抽出スコープの最適化（タスク6完了）**
- [x] `propose_topics.py` の抽出プロンプトを全面改良。「良い例・悪い例」を明示し、AIが **エッセイの核心的主張（`essay_claim`）** を1文で要約してからトピックを設定する3ステップ手順に変更。
- [x] `essay_claim` を初期コンテンツ生成にも連携させ、「議論されている主なポイント」がエッセイの問題提起に直接答える構成になるよう改良。

**5. PRエージェントの `topic.yaml` 対応（タスク1に向けた機能拡張）**
- [x] `pr_comment_agent.py` を拡張し、`.md`（記事文章）だけでなく `topic.yaml`（タイトル・キーワード・ndl_keywordなど）もPRコメントで修正できるようにした。
- [x] `id`・`slug`・`status`・`created_at` は変更不可とする安全ロックを実装。

**6. `propose_topics.py` の重大バグ修正**
- [x] `propose_via_github()` に `topic_data` と `essay_url` が渡されておらず `NameError` で落ちていたバグを修正。
- [x] ファイル末尾に `main()` が2回記述されており、2回目の実行時にJSON解析エラーが起きていたバグを修正。
- [x] フォルダが既に存在する場合に `None` を返してPR作成まで到達できなかった問題を修正（前回失敗分も再試行できるように）。

---

## 📌 積み残し・次回やること（Next Actions）

**1. PR自動修正フローの実稼働最終確認**
- [ ] `@ronten-ai` コメントによる `.md` / `topic.yaml` 両方の修正が本番環境で正しく動作するかテスト。
- [ ] `propose_topics.py` のバグ修正後、議席削減トピックのPR作成を再実行して確認。

**2. GAS と GitHub Actions の連携（AXIS 2）の完了**
- [ ] GitHub Secrets への `GEMINI_API_KEY` の設定と連携確認。
- [ ] ユーザーからの情報提供フォーム（Google Forms → GAS → GitHub Actions）の完全な End-to-End テスト。

**3. PRコメント時のトリガー（`@ronten-ai`）要否の再検討**
- [ ] より手軽にAIに修正を指示できるUXの検討（メンションなしでの自動反応や短縮トリガーの導入など）。

**4. 記事生成品質の継続監視**
- [ ] 「平易な表現」「AI特有の言回し排除」のルールが全自動生成記事に漏れなく適用されているか監視・チューニング。

**5. 編集方針・ディスクレーマーの強化**
- [ ] 読者からの情報提供について「編集側の判断で選別・編集される場合がある」というポリシーを明文化。

**6. 記事内での「元エッセイ要約」の表示**
- [ ] 記事の「関連エッセイコラム」の下などに、起点となったエッセイの要約（筆者の問題意識）を3行程度表示するセクションを設ける。
- [ ] 「エッセイの問題提起」→「客観的なファクトが答える」という構成への記事フォーマット見直し。
