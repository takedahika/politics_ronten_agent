# 論点の現在地 — 進捗まとめメモ（統合版）
> 2026-08-11 更新

プロジェクト「論点の現在地」の開発進捗、今後のタスク、および方針として見送った事項のまとめです。

---

## ✅ できたとこ（完了した実装）

### 1. ステートフルな情報収集アーキテクチャの確立
- `topic.yaml` に `kokkai_last_date` / `news_last_date` を記録し、前回取得分以降の情報をページネーションで取得する仕組みを実装。
- 10件ずつ取得するバッチ処理により、AIのコンテキスト溢れを防ぎ、「徐々に文脈が育っていく」思想を実現。

### 2. トピック・論点の抽出精度の向上
- ニュースレター（エッセイ）からの新トピック提案時（`propose_topics.py`）に、Google Search Groundingを導入。
- 「論点の現在地」などのメディア名やブログ名を論点として誤抽出しないようプロンプトを厳格化。
- トピック生成時の日付や事実関係の出典元を、政府機関・公的機関・裁判所・主要報道機関などの一次資料に限定。

### 3. 政治的コンテキスト・スタンス抽出の厳格化
- `analyze.py` で情報を追加する際、「一般的な党のイデオロギー」ではなく、「特定のトピックに対する具体的なスタンス（推進・反対など）」のみを抽出するようにプロンプトを改修。
- NPOや弁護士会などの「批判側の声明」を根拠に、他党の発言を事実としてでっち上げる挙動を禁止。必ずその発言自体の一次資料（国会議事録や大手ニュース）を出典とするよう制限。
- Wikipedia、個人のブログなどの非信頼ソースを事実として引用することを禁止。

### 4. AIのメタテキスト排除と表示順序の修正
- 「AIがまとめました」等のAI自身による挨拶・言及をすべての出力から排除。
- タイムライン（`timeline.md`）において、ニュース記事の配信日ではなく「実際の出来事・発表が起きた日」を厳格に取得するように指示。
- トピックの更新時、最新の出来事がヘッダー直下（降順）に挿入されるロジックを実装。

### 5. 安定化・クリーンアップ
- Discord通知におけるエラーを解消し、`jq` を用いた安全なJSON生成方式へ移行。
- ダミー情報を含んでいたテスト用トピックやキャッシュファイル（`.processed_essays.json`）の削除・リセット。
- JSONパースエラー時のサイレントフェイル（無言での処理済みキャッシュ登録）を防止するエラーハンドリングを追加。

---

## 🔲 これからやるとこ（未完了・直近のタスク）

GitHubの画面から、ユーザーが柔軟にここを変更してほしいという内容を丸っと出せるような仕組みを作りたいなと思っています。それを明日やることのメモのところに追加しておいて。

### 1. UIおよびGAS連携の強化（ニュースレター連動）
- `topic.yaml` に実際のニュースレター（エッセイ）のタイトルとURLを保持し、Webサイト（Next.js）側で動的にそのリンクを表示する。
- サイトUIの固定文言（「エッセイ『1年に3日だけ政治を考える』で取り上げられました。」など）を、動的で汎用的なテキストに変更する。
- **Substackの自動検知（GAS実装）**:
  Substackのスクレイピングがブロックされるため、GmailのSubstackメールを受信トリガーにして本文を抽出し、GitHub Actionsへ送るGASプロジェクト（`02_Homemade_Apps/34_substack_gmail_trigger/`）を完成させる。

### 2. 【Post-MVP】ユーザー参加型（UGC）ファクト追加機能の構築
- サイトを訪れた一般ユーザーが、特定の論点に対して新しい事実やソースを投稿（提案）できるフォームの設置。
- 送信内容をAIが検証・成形し、Pull Requestとして自動作成。管理者がレビューして反映するフローの構築（スパム対策とファクトチェック精度の担保が必要）。

### 3. 【軸3】GitHub PR画面上でのAI対話・自動修正機能
- AIが作成したPRに対し、人間がコメント（例：`@AI ここの表現を変えて`）するだけで、AIが自動で内容を修正し追加コミットをプッシュする仕組みの構築。
- GitHub Actionsの `issue_comment` イベントをトリガーとし、ローカル環境を介さずにスマホ等からでも完璧な記事編集が行えるようにする。


---

## 🚫 今はやらないとしていること（見送り・方針転換）

### 1. ニュースSNSボット化計画（完全廃棄）
- ThreadsやTwitterにニュースのまとめやコメントをツリー状に投稿し、ニュースレターへの送客を狙う計画。
- **見送り理由**: 単なるニュースの羅列では他チャンネルとの差別化が難しく、コンテキストが失われるため。
- **現在の方針**: SNSボット化は完全に廃棄し、まずは「論点の現在地」のコアであるWebサイトとMarkdownデータベースの充実（本質的な政治的文脈の可視化）に100%注力する。

### 3. 自動のファクトチェック警告・CI機構
- AIが出力したソースの欠落などを自動検知して警告を出すスクリプトやフラグ機能の実装。
- **見送り理由**: 全ての更新は元々GitHubのPull Requestとして人間が目視で確認・承認するフローになっているため、システム側で過剰な自動警告機能を作り込む必要性は薄いと判断。
- 国会APIやニュースを一度に100件など大量に取得するアプローチ。
- **見送り理由**: AIのコンテキスト上限や分析の粗さに繋がるため、過去に遡って10件ずつループ取得する方針に切り替え済み。
